# 业内因子挖掘平台：因子构建与筛选流程梳理

> 本文整理自对某业内因子挖掘平台（下称「平台」）的调研，内容来自平台的帮助文档、实验室页面和因子字典数据，
> 用作本仓库「量化因子筛选库」的设计参考。文中数据只反映调研时的情况，不构成投资建议。

---

## 1. 总览：五步流水线

平台把股票（指数增强）和公募基金（FOF 增强）两条业务线做成了同一套五步流水线：

```
                ┌──────────────────────── 因子来源 ────────────────────────┐
                │  传统因子库（标普 Alpha 因子库 + 券商研报因子）                │
                │  遗传规划挖掘（符号回归）→ 初筛 → 基因工厂物化入库               │
                └────────────────────────────┬─────────────────────────────┘
                                             ▼
 ① GA 挖掘 ──► ② 因子批测 ──► ③ 因子回顾/筛选 ──► ④ 因子合成 ──► ⑤ 优化器 ──► 组合 / 业绩回测
              (IC/分组/TopK)   (显著性+门槛+去相关)  (线性/ML/LSTM/DRL)  (跟踪误差/行业偏离/个股上限)
```

| 步骤 | 做什么 | 产出 |
| --- | --- | --- |
| ① GA 挖掘 | 在原始因子上用遗传规划进化公式，初筛后按月物化成因子截面 | GA 因子库（含样本内 / 样本外指标） |
| ② 因子批测 | 传统因子与 GA 因子放在同一套口径下统一回测 | 回测结果库：IC、ICIR、p 值、分组收益、多空、Top-K 超额 |
| ③ 因子回顾 | 按显著性、方向、风格、业绩门槛过滤，勾选进「备选因子池」 | 备选因子池 |
| ④ 因子合成 | 把备选池合成为一个综合因子 | 合成因子（combo） |
| ⑤ 优化器 | 单因子细致回测；多因子用凸优化控制相对基准的偏离 | 月度 / 周度持仓权重 |

另有两条辅助线：

- **风险因子批测 → 市场风格**：持续计算 Barra 风格暴露，用来理解 Smart Beta 和做风格归因。
- **高频因子**：基于 Level-2 数据单独计算，结果汇总展示。

---

## 2. 数据组织

### 2.1 存储结构

平台用文档型数据库存因子，结构为：

```
collection（因子大类 / 风格）
  └── document（一个截面日期）
        └── 字段值：{ 股票代码: 因子值, ... }
```

- 月频因子存**月末截面**，日频因子存**交易日截面**；回测时统一对齐到月末。
- 行情、复权价格、股票池 / 基金分类单独存储，与因子库解耦。
- 回测结果单独放一个库，每个「批测方案」一个集合，因子结果的 key 形如 `股票池|风格|因子名`。

### 2.2 股票因子库构成

传统因子主体来自**标普 Alpha 因子库**，共 **429 个因子，分 8 个风格**：

| 风格 | 数量 | 含义 |
| --- | --- | --- |
| 盈利质量 Earnings Quality | 100 | 应计、利润率、现金流与利润匹配度 |
| 历史成长 Historical Growth | 80 | 营收 / EPS / 现金流的多期增长率与增长动量 |
| 估值 Valuation | 79 | EP / BP / CFP / EV 等倍数及其预期修正 |
| 资本效率 Capital Efficiency | 67 | ROIC / ROA / ROE、各类周转率 |
| 价格动量 Price Momentum | 62 | 短中长期收益、RSI / MACD、成交量信号 |
| 分析师预期 Analyst Expectations | 27 | 目标价、EPS / 营收一致预期及其修正 |
| 波动率 Volatility | 10 | 实现波动、下行风险、Amihud 非流动性 |
| 规模 Size | 4 | 对数市值、市值三次方等 |
| AI 因子 | 预留 | GA / 机器学习合成的因子 |

此外还接入了几家券商研报里的因子，按来源分类展示。

### 2.3 命名约定：因子数量是怎么「扩」出来的

429 个因子并不是 429 个独立想法，而是**少量基础指标 × 口径 × 窗口 × 相对化方式**的组合：

| 标记 | 含义 |
| --- | --- |
| `IndRel_` 前缀 | 当日行业内截面标准化，剔除行业均值，只保留行业内相对排序 |
| `5YRel_` 前缀 | 相对自身过去 5 年的 z-score |
| `_TTM` / `_LYR` / `Fwd` | 滚动四季度 / 最近年报 / 一致预期前瞻口径 |
| `Chg` / `Mom` | 变动量（同比、环比）/ 趋势斜率 |
| `3M` `6M` `12M` `24M` `52W` `90D` `130D` | 计算窗口 |

例：`PM5D`（5 日价格动量）→ `IndRel_PM5D`（行业相对 5 日动量）；`OCFP`（经营现金流市值比）→ `5YRel_OCFP`。

### 2.4 股票池

每个因子在多个股票池里**分别测试**：沪深300、中证500、中证1000、中证2000、全市场。
同一个因子在不同池子里的表现差别可能很大，所以筛选也按股票池分开做。

---

## 3. 单因子测试口径（因子批测）

### 3.1 指标定义

| 指标 | 定义 |
| --- | --- |
| IC | 因子值与**下期收益**的 Spearman 秩相关（Rank IC），逐期计算得到 IC 序列 |
| ic_mean / ic_std | IC 序列的均值 / 标准差 |
| IC_IR | `mean(IC) / std(IC)`，衡量稳定性 |
| p 值 | 对 IC 序列做均值 t 检验：< 0.05 较显著，< 0.01 高度显著，≥ 0.05 慎用 |
| IC 正比率（胜率） | IC 方向与因子方向一致的期数占比 |
| 分组回测 | 每月末按因子值排序，等分 N 组（默认 5 组），看下一期各组收益 |
| 多空年化 | `LS = top组收益 − bottom组收益`，按月复利年化 |
| 多空夏普 / 回撤 | 多空序列的夏普（Rf=0）与最大回撤 |
| Top 组超额 | Top 组年化收益 − 池内等权基准年化收益 |
| 超额波动 / 超额回撤 | Top 组逐月超额序列的年化波动与最大回撤 |
| 信息比率 | Top 组年化超额 ÷ 超额年化波动 |
| Top-K 组合 | 按因子取前 K 只（绝对数或百分比）等权持有，只影响组合结果，不影响 IC 计算 |

### 3.2 参考阈值

平台文档引用的经验值：

- |Rank IC| < 0.03：可靠性不足；≈ 0.05：较好；> 0.07：很强
- |ICIR| > 0.5：较优

### 3.3 实测情况

平台对标普因子库的测试结果：

- 397 个有结果的因子里，只有 **65 个 p < 0.05**（约 16%）
- 显著因子分布：价格动量 17、估值 13、盈利质量 12、历史成长 10、分析师预期 7、资本效率 4、波动率 2
- ICIR 靠前的因子（沪深300 池）：

| 风格 | 因子 | 含义 | IC | ICIR |
| --- | --- | --- | --- | --- |
| 价格动量 | 5DVolSig | 5 日成交量信号 | −0.055 | −0.49 |
| 历史成长 | SusGrwRate | 可持续增长率 | −0.026 | −0.43 |
| 价格动量 | IndRel_50DVolSig | 行业相对 50 日成交量信号 | −0.041 | −0.42 |
| 价格动量 | IndRel_PM5D | 行业相对 5 日价格动量 | −0.040 | −0.39 |
| 价格动量 | PM5D | 5 日价格动量 | −0.040 | −0.36 |
| 估值 | 5YRel_OCFP | 5 年相对经营现金流市值比 | +0.023 | +0.36 |
| 盈利质量 | IndRel_CashCycle | 行业相对现金循环周期 | +0.034 | +0.33 |
| 估值 | FwdFCFPC | 前瞻自由现金流收益率 | +0.019 | +0.30 |

**结论**：大部分「教科书因子」单独用在 A 股上并不显著，A 股上短期反转、量能类信号更强，筛选这一步必不可少。

---

## 4. 因子筛选规则

### 4.1 通用过滤

因子回顾页面提供的过滤维度：

- 批测方案、股票池、风格分类
- 方向：正向 / 反向因子
- **只看显著（p < 0.05）**

### 4.2 指数增强的族内筛选（默认参数）

| 门槛 | 默认值 |
| --- | --- |
| 历史年化超额 | ≥ 2% |
| 今年超额 | ≥ 0 |
| 近 12 月超额回撤 | ≥ −10% |
| 样本月数 | ≥ 48 |
| 族内去相关 | 两因子 \|ρ\| ≥ 阈值时二选一 |
| ICIR | ≥ 阈值 |
| 每族最多保留 | 前 N 个（按档位 1–4 调整 N） |

族内排序打分 = 历史超额、今年超额、回撤、ICIR 四项**加权求和**，权重可调。

### 4.3 风格族配权

筛完后，默认**五大风格族各占 20%**，族内等权合成一个 combo 因子（「风格等权合成」）；
也可以去掉某一族或改占比。另一条路径是把精选因子直接送入 LSTM。

---

## 5. GA 因子挖掘（遗传规划）

### 5.1 原理

用 `gplearn` 做符号回归：

- **叶子节点**：原始因子
- **函数节点**：加减乘除、截面排名、时序统计（滚动相关、协方差、时序排名、衰减加权等）
- 在表达式树上做交叉、变异、复制，逐代进化出新的组合因子公式

### 5.2 适应度（三选一）

| metric | 目标 |
| --- | --- |
| `rank_ic` | 因子排序与未来收益排序一致 |
| `top_return` | 头部组合（多头端）的收益 |
| `rank_ic_with_top_return` | 两者折中 |

### 5.3 防过拟合：样本内 / 样本外切分（ITS / OFS）

- `max_samples`（0.1–1.0，常用 0.5–0.7）：按**时间**切分月度截面，前段算样本内适应度（ITS），后段做样本外检验（OFS）
- 例如 0.6 表示前 60% 的月份参与进化，后 40% 只做检验
- `parsimony_coefficient`：公式长度惩罚，抑制无意义膨胀
- `min_observations`：原始因子至少要有这么多非缺失值，才能进入特征池

### 5.4 每个公式记录的指标

| 字段 | 含义 |
| --- | --- |
| `its` / `ofs` | 样本内 / 样本外适应度 |
| `its_ic_hit` / `ofs_ic_hit` | IC 方向正确的期数占比 |
| `its_ic_apc` / `ofs_ic_apc` | IC 均值 / 标准差 |
| `generator` | 产生于第几代 |
| `ga_params` | 超参快照，用于复现 |

### 5.5 主要超参

| 参数 | 作用 |
| --- | --- |
| `generations` | 进化代数 |
| `population_size` | 每代个体数 |
| `hall_of_fame` | 名人堂容量（≤ 种群规模） |
| `n_components` | 去相关后最终保留的公式条数 |
| `tournament_size` | 锦标赛选择规模，越大选择压越强 |
| `init_depth` | 初始公式树深度范围 |
| `p_crossover` / `p_subtree_mutation` / `p_hoist_mutation` / `p_point_mutation` | 交叉 / 子树变异 / 提升变异 / 点变异概率，四者之和 ≤ 1，余下为复制 |
| `p_point_replace` | 点变异中替换叶子节点的比例 |
| `random_state` / `n_jobs` | 随机种子 / 并行数 |

时序算子需要 **20 个月 warmup**，样本区间至少 21 个月。

### 5.6 初筛与去重

1. **初筛**：按样本内 IC 下限、样本内胜率下限、样本外 IC 下限、样本外胜率下限过滤
2. **去近似重复**：6 项指标（its、its_ic_hit、its_ic_apc、ofs、ofs_ic_hit、ofs_ic_apc）全在容差内相等，就视为同一个因子，**保留公式更短的**

   | 指标 | 容差 |
   | --- | --- |
   | its / ofs | 0.002 |
   | its_ic_apc / ofs_ic_apc | 0.05 |
   | its_ic_hit / ofs_ic_hit | 0.01 |

3. **基因工厂**：入选公式按月物化成因子截面入库，支持续跑补新月份；之后和传统因子一起进入统一批测

### 5.7 延伸：学术参考

平台文档引用了两个方向的文献，但当时均**未完整接入**生产流程：

- **SGP-LSTM**（Li Q. 等, *Scientific Reports*, 2023）：先用符号遗传编程合成因子，再用 LSTM 做截面收益排序预测（下一期收益是否高于截面中位数）
- **BayGA**（Li Q. 等, *Scientific Reports*, 2025）：贝叶斯优化 + 遗传算法，自动调深度网络的超参数（层数、节点数、Dropout、学习率），目标为验证集 Rank IC

---

## 6. 因子合成

| 方式 | 说明 |
| --- | --- |
| 线性 · ICIR 加权 | 以各因子 ICIR 作为权重线性合成 |
| 线性 · 因子等权 | 统一方向后等权 |
| 机器学习 | Random Forest / GBDT / XGBoost / LightGBM / Ridge / Elastic Net；标签为未来 N 个月复合收益；Train / Val / Test 按时间切分，Val 做早停，报告 Test 集 IC 与组合超额；以等权合成为基线 |
| LSTM | 输入滞后 1–3 期的因子序列，回归 forward return 或截面 rank（Huber / MSE 损失），默认 70 / 20 / 10 切分，Val 早停 |
| 深度强化学习 | 用 ML 合成因子选股后，送入 PPO / SAC 做仓位决策 |

所有合成方式都保存成新的因子截面，可以像单因子一样再做回测。

---

## 7. 优化器（指数增强）

| 项目 | 设置 |
| --- | --- |
| 基准 | 沪深300 / 中证500 / 中证1000 / 中证2000 |
| 股票池 | 仅成分股，或允许部分成分外（成分内 ≥ 80%） |
| 约束 | 个股权重上限、跟踪误差、行业偏离 |
| 求解器 | MOSEK / CVXOPT |
| 调仓 | 月频 / 周频 |
| 评价 | 相对市值加权指数的逐年超额、超额波动、超额最大回撤、累计超额 |

---

## 8. 对本仓库的启示（以世坤 Alpha101 为起点）

1. **统一数据格式**：面板结构 `(date, code) → value`，因子、收益、股票池、行业分类分开存储。
2. **因子扩展器**：对 Alpha101 输出统一生成变体（原值 / 行业内标准化 / 自身历史 z-score / 多窗口），因子数可以扩几倍。
3. **预处理**：去极值（MAD）→ 标准化 → 行业 / 市值中性化（Alpha101 原文大量使用 `IndNeutralize`）。
4. **单因子测试**：Rank IC、ICIR、t 检验 p 值、IC 胜率、分组回测、多空、Top-K 超额，再加换手率；按多个股票池分别跑。
5. **筛选**：显著性 + 超额 / 回撤 / 样本长度门槛 + 相关性去重 + 样本内 / 样本外一致性 + 按风格族限额。
6. **合成**：先做等权和 ICIR 加权基线，再接 LightGBM 等机器学习，最后再考虑深度学习。
7. **GA 挖掘（可选）**：用 Alpha101 的算子（`rank`、`ts_rank`、`correlation`、`decay_linear`、`delta` 等）作为 gplearn 的函数集，以 ITS / OFS 切分、长度惩罚和指标去重控制过拟合。
8. **优化器**：用 cvxpy 实现带跟踪误差、行业偏离、个股上限约束的指数增强组合。
