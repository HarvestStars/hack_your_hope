# 业内量化投研平台：业务架构调研报告

> 调研对象：一家面向个人投资者的量化投研 SaaS 平台（下称「平台」）。
> 调研时间：2026-09-24。资料来源：平台的前端页面、站内帮助文档和只读数据接口。
> 文中数字只反映调研当天的情况，不构成投资建议。
>
> 本文有两个目的：
> 1. 把这个平台的业务梳理清楚（§1–§7）。
> 2. 从中抽象出**量化投研平台的通用工作流架构**（§8），作为自研平台的全局蓝图（§9）。
>
> 因子挖掘部分另见专题：[factor_mining_topic.md](factor_mining_topic.md)

---

## 1. 一句话定位

平台把「看市场 → 写投研 → 出权重 → 建组合 → 公开 / 跟单 / 模拟交易」做成一条闭环，
每个环节都配了一个大模型驱动的 AI 投资助手（AI-Manager），靠会员年费和大模型用量计费。

因子挖掘只是其中「出权重」这一层里的两条线（公募 FOF 增强、股票指数增强），不是平台的全部。

---

## 2. 总体架构

```text
┌──────────────────────────────────────────────────────────────────────────────┐
│ 商业化层   会员四档年费 · 平台币（1 币 = 1 元，按大模型用量扣）· 数据下载配额        │
├──────────────────────────────────────────────────────────────────────────────┤
│ 社交/交易层 竞技场（公开组合、实时跟单、生成式 AI 策略） · 全球交易（券商模拟盘）      │
│            · 公募基金交易 · 另类市场（赛马、未来事件，美元模拟）                     │
├──────────────────────────────────────────────────────────────────────────────┤
│ 组合层     我的组合：组合构建 → 业绩展现 → 按冻结参数自动续跑调仓                    │
├──────────────────────────────────────────────────────────────────────────────┤
│ 策略实验室 资产配置(SAA/TAA) · 公募FOF增强 · 行业轮动 · 股票指数增强                 │
│            · 私募FOF(MOM) · 固收+(CPPI/TIPP)                                     │
├──────────────────────────────────────────────────────────────────────────────┤
│ 研判层     市场观察（日报、资讯、宏观、温度计） · AI投研（多智能体研报）              │
├──────────────────────────────────────────────────────────────────────────────┤
│ 公共能力   AI 助手（共驾 / 自动驾驶） · 大模型网关与算力池 · 回测引擎 · 优化器        │
├──────────────────────────────────────────────────────────────────────────────┤
│ 数据层     MongoDB：行情/净值、因子库、研报库、回测结果库(res_bt)、GA 因子库          │
│            · 外部：券商行情接口、基金销售平台、赛马赔率、预测市场行情                │
└──────────────────────────────────────────────────────────────────────────────┘
```

技术栈：后端是 Python（Flask/Werkzeug），前端是单页应用（原生 JS + Plotly），
数据主要存在 MongoDB，高频数据用 Parquet。
长任务（批测、GA、训练）在后台单进程跑，前端每 0.8–1.5 秒轮询一次日志；
同一类批量任务同一时间只允许跑一个。

---

## 3. 主业务流

平台自己的说法是「看市场 → 写投研 → 配仓 / 增强 → 组合落地 → 竞技场」。展开如下：

```text
 ┌──────────┐   ┌──────────┐   ┌─────────────────────────┐   ┌──────────┐   ┌──────────────┐
 │ 市场观察  │──►│ AI 投研   │──►│ 策略实验室（出权重）      │──►│ 我的组合  │──►│ 竞技场        │
 │ 日报/资讯 │   │ 只出报告  │   │ SAA/TAA · FOF · 行业      │   │ 构建/业绩 │   │ 公开/跟单     │
 │ 宏观/温度 │   │ 不出仓位  │   │ 股票增强 · MOM · 固收+    │   │ 自动续跑  │   │ 生成式AI策略  │
 └──────────┘   └──────────┘   └─────────────────────────┘   └────┬─────┘   └──────┬───────┘
                                                                  │                │
                                                                  ▼                ▼
                                                          ┌──────────────────────────────┐
                                                          │ 全球交易 / 公募交易（模拟下单）│
                                                          └──────────────────────────────┘
```

关键的分工约定：

- **AI 投研只研判，不给仓位**。要落成权重，必须去对应的实验室。
- **实验室只用来做新策略**。组合一旦创建，就用创建时冻结的参数按月（或按年）续跑，不用回实验室重跑。
- **跟单、交易一律是模拟资金**，不连真实成交（公募交易除外，见 §4.11）。

---

## 4. 业务模块

顶栏共 12 个模块，下面按主业务流的顺序介绍。完整的导航表见附录 A。

### 4.1 市场观察（每天开盘先看）

| 子页 | 内容 |
| --- | --- |
| 每日点评 | 整合国内媒体、全球视角、共识前瞻、宏观、宽基、资金估值、波动，由大模型生成结构化日报，可导出 PDF 或 HTML，一次约 3–4 分钟 |
| 共识前瞻 | 从预测市场里筛出政治和金融事件，自上而下推导出对 A 股、中美债、加息、贵金属、原油、地缘的市场共识，每天抓取后自动生成 |
| 资产观察 | 大类资产日观察：宏观边际变化 → 权益 / 固收 / 黄金的配置方向，给出每日观点变化和情景推演 |
| 国内媒体 / 全球视角 | 抓取资讯后按方向矩阵归纳；全球视角走「宏观指标 → 五大区周期 → 政经事件 → 各类资产」的顺序写综述 |
| 市场温度计 | 宽基和中证一级行业的 YTD / MTD 表现、股指期货升贴水；另有宏观数据多国横向对比、北向南向资金、两融、ETF、估值水位、期权波指和 VIX |
| 宏观交易 | 生成「宏观 → 中观 → 微观」研判和交易机会（股票、利率、期权、期货、海外），可一键推送到模拟交易台 |

右侧常驻 AI 助手，只根据当前页已经生成的内容回答问题，不会重新跑报告。

### 4.2 AI 投研（多智能体研报流水线）

四个实验室，产出都是**报告 / PDF，不是仓位**：

| 实验室 | 流水线 | 产出 |
| --- | --- | --- |
| 宏观周期 | 分析包（价格统计 + 市场展望 + 上一年留下的记忆）→ 从研报抽观点 → 异构专家会诊（凯恩斯、技术、资金、政策、行为）→ 聚合出周期判断 → AI 写总结 | 周期阶段判断 + PDF |
| 资产轮动 | 五个智能体直接给股、债、商品、货币打分，不先判断宏观阶段 | 0–100 的大类强弱指数 |
| 行业轮动 | 抽研报 → 会诊 → 聚合 → 生成式因子 | 0–100 的行业强弱指数 |
| 金股点评 | 读券商每周金股 / 月度金股组合 → 抽出标的 → 五专家会诊（宏观、政策、基本面、资金、主题）→ 合成共识清单 | 周评 |

每次运行前先选大模型算力通道：用户自带的 API，或平台的共享算力池（按次数加时长计费）。

### 4.3 资产配置（SAA / TAA）

| 层级 | 做法 | 产出 |
| --- | --- | --- |
| **SAA 战略配置**（年度） | 四阶段闭环：① 数据摄取和上下文（价格、研报、持续记忆）→ ② 两阶段智能体推理（可增删的六类专家：凯恩斯、量化趋势、宏观周期、流动性、政策、行为金融），输出普林格阶段概率和资产超额观点 → ③ 投票聚合 + **Black-Litterman** 融合观点 Q、置信度 Ω 和市场先验 Π → 优化器出权重 → ④ 实际结果落地后做「实际 vs 预测」反思，写成下一年的记忆 | 大类资产的年度中枢权重 |
| **TAA 战术配置**（月度） | 以年调的风险平价（RP）或均值方差（MV）权重为锚，用因子给股、债、商品、现金打分，按月做相对倾斜，约束 \|仓位 − 中枢\| ≤ max_dev（3%–20% 可选）。因子库共 73 个，国内生产默认 24 个、国际 32 个，分六大风格：动量、波动、国内宏观、海外宏观、估值、Carry。默认合成方式是风格等权、风格内等权 | 月度权重 |
| 配置回顾 | 普林格 6 阶段 / 美林 4 象限时钟的命中率回测（宏观变量、阶段、股债相对、大类顺序）；「时钟修正风险平价」按稳健、平衡、进取分档，可同步到竞技场 | 回顾报告 |
| 保险公司配置总结 | 自上而下：SAA（未来 1 年）→ TAA（四视角打分 −100~100）→ 国内 / 全球倾斜，出正式报告 | PDF |

SAA 和 TAA 保存方案后有两个去向：我的组合（配置组合）或全球交易的量化台（按 ETF 一揽子下单）。

### 4.4 公募 FOF

两条路：

- **常规选基**：
  - 查基金：净值、风险收益、AI 点评。
  - 选经理：分主动权益、FOF、固收+、纯债、量化几类，按多维能力雷达打分，看代表基金，可多人对比，有 AI 点评。
  - 选中的基金加入「FOF 待选池」，再到组合构建里按等权、MV 或 RP 配权。
- **多因子增强**：五步流水线（GA → 批测 → 回顾 → 合成 → 优化器）。详见[因子专题](factor_mining_topic.md)。

  另有 AI 助手一键模式：选好 ML 合成方案后，对持仓做穿透，用 LP 优化，基准是中证偏股基金指数（930950），出历史业绩。

### 4.5 行业轮动（11 个行业月度相对配置）

行业池：银行、非银、地产、煤炭、有色、食品饮料、家电、TMT、医药、军工、传媒。
只在权益内部做相对强弱，不替代 SAA。

| 模型 | 流程 | 对照基准 |
| --- | --- | --- |
| A. 深度强化学习（价量） | 从指数行情构造 6 组行业因子 → 截面 Spearman RankIC（前三组 RankIC > 2% 才入选，后三组只做诊断）→ 月度等权基线 → 当场训练 PPO/SAC（网页默认 2 万步，离线 36 万步 + 早停，PPO 开 10 个并行环境）→ 回测。可调参数：因子组、算法、是否含现金（年化 2%）、reward（组合收益 / 夏普 / 带波动惩罚的收益） | 等权 |
| B. 大语言模型（研报） | 行业研报（强制发布日 ≤ 信号日）→ 按宏观、政策、盈利、资金、舆情五个视角分别抽取并独立打分 → 聚合出期望收益、置信度、一致度 → 映射成生成式因子 → 在风险平价基础上倾斜 3% / 5% / 8% | 年调 RP |

两条路各自保存为独立组合，不串联。

### 4.6 股票增强（指数增强）

- **常规研究**：查股票（走势 + AI 点评）；市场风格（Barra CNE5 的 10 个风格因子的暴露热力、分组累计收益、AI 点评），靠「风险因子批测」保持更新。
- **五步因子流水线**：GA → 批测 → 回顾（传统 / GA 精选 / 高频）→ 合成（AI / 线性）→ 优化器（单因子 / 多因子）。
- **AI 助手一键增强**：选指数（300 / 500 / 1000 / 2000）→ 选风格族和权重 → 族内筛选 → 合成 combo → 优化器，分共驾和自动驾驶两种模式。

**这部分就是因子挖掘的主战场，完整拆解见 👉 [因子挖掘专题](factor_mining_topic.md)。**

### 4.7 私募 FOF（MOM）

**不走因子流水线**，走的是「赛道 → 产品 → 选小 M → 组合」。「小 M」指 MOM 结构里的子管理人或子产品：

1. 策略总览：按投资风格和子策略看收益分布、风险收益散点、赛道汇总（2019 年至今）。
2. 查产品：单产品看净值、超额、AI 点评；最多 10 只放在一起对比（累计净值、相关矩阵、按信息比率排序）。策略类型分股票增强、主观多头、债券增强、绝对收益、另类。
3. 比业绩：读取「私募批量回测」（方案名以 `mom` 开头）的结果集，按策略勾选多个小 M 进待选池。
4. 组合构建：按等权、MV 或 RP 配权。

### 4.8 固收+（CPPI / TIPP）

资产只有两类：权益和交易债（国内用中证综合债，海外用美债）。

- CPPI：地板不动，安全垫随盈利变厚。TIPP：地板随净值上移，只升不降，会锁住浮盈。权益仓 W ≈ m × C / V。
- 默认极端回撤 risk_dd = 24.8%（十年一遇压力），乘数 m = 1 / risk_dd ≈ 4，权益仓上限 35%，另有月度硬止损。
- 四步流程：
  1. 基准回测，用指数做权益和债的「袖套」。
  2. 从公募 FOF 引入权益增强策略和纯债策略，替换袖套的收益来源。
  3. 用同样的参数重跑组合回测，权益和固收的占比规则不变。
  4. 按当期建议仓位生成场外基金持仓，建立组合。

### 4.9 我的组合（所有实验室的共同出口）

| 组合类型 | 来源 | 配权方式 |
| --- | --- | --- |
| 配置组合 | SAA / TAA | 实验室给出权重 |
| FOF 组合 | 查基金、选经理、线性加权、增强优化器 | 等权 / MV / RP |
| 股票组合 | 单因子策略、多因子策略 | 实验室给出权重 |
| MOM 组合 | 比业绩待选池 | 等权 / MV / RP |
| 行业轮动 | DRL / LLM | 实验室给出权重 |
| 固收+ | CPPI / TIPP | 当期建议 |

**续跑机制**是这一层的核心设计：

- 组合按**策略指纹**（资产、因子、方法等参数）识别，不按名字。参数一改就算新策略，必须另存。
- 调仓频率：TAA 和行业轮动按月，SAA 按年。自动调仓在到期工作日 16:35 执行；量价类直接落账，研报类和 SAA 先出草案，确认后再写入。
- 只追加调仓记录、不改写历史净值；调仓日不能早于上一次调仓。
- 构建日之前是样本内回测，之后是样本外模拟。配置组合的样本外用真实指数收盘价估值。
- 股票增强、FOF 增强、MOM 有各自的调仓逻辑，不走上面这套「按同参数找回」的续跑。

### 4.10 竞技场（社交投资广场）

- **发现组合**：公开组合的卡片墙，按品类筛选（平衡型 FOF、权益增强 FOF、行业轮动 FOF、固收+ FOF、债券型增强 FOF、股票增强、私募 FOF），也能按策略类型、风险、年龄段筛；按最新、人气、收益、跟单数、抗跌排序。
- **实时跟单**：用模拟资金，默认 10 万，最少 1 千；止损默认 −20%，可在 5%–80% 之间调；每人最多同时跟 20 个。源组合调仓会按权重同步过来。另有「仅复制持仓」，一次性拷贝、之后不再同步。
- **生成式 AI 策略**：用户用对话说出策略类型、最大回撤、风格，系统只从公开的「叶子策略」里挑选并配权回测。基础策略调仓时自动传导；生成出来的组合不能再被当作下一层的基础策略。
- **交易员 / 动态 / 我的跟单**：关注、时间线、跟单管理（暂停、恢复、加仓、改止损、停止）。
- 卡片上的「风险分 1–10」主要按最大回撤估算。公开组合前必须先设置展示昵称。

### 4.11 全球交易

- **券商模拟盘**：服务端替用户托管券商的 OpenAPI 网关，只做模拟盘。分三个台：
  - 主观交易：逐笔下单，看 K 线和资讯。
  - 量化交易：以 TAA 方案为单位，按 ETF 一揽子下单，在组合和个券两层监控。
  - 宏观交易：接收宏观交易研判推送来的方案，多腿、多币种批量下单。
- **公募交易**：用户扫码登录第三方基金销售平台的账户，可以单笔申购，也可以把竞技场组合「一键下单到公募」后一揽子申购。**这是全站唯一可能产生真实交易的入口。**

### 4.12 另类市场（美元模拟，娱乐加研究）

- **赛马交易所**：香港赛马的即时赔率。独赢和前三的概率各用一个 LightGBM 模型预测，可选 Platt 校准；Edge = 模型概率 − 市场隐含概率（1/odds 归一）；按锁定时的赔率结算。
- **未来事件交易所**：同步外部预测市场的二元事件。两边都有人下注时按彩池分派；只有一边有注时，按入场隐含概率对庄结算。另有 AI 写的「未来故事」。
- 每年 1 月 1 日发 10 万美元模拟金。当年年化 > 10% 且夏普 > 1.5 的用户，次年免费升中级会员。这是一个拉活跃的设计。

### 4.13 帮助和数据

板块使用说明、会员权限、大模型网关说明、因子定义与测试方法、三本因子字典（基金、股票、大类资产）、
三篇算法说明（GA、SGP-LSTM、BayGA）、竞技场和组合说明、高频数据说明（Level-2 因子目录和 Parquet 样本浏览），
以及数据下载（导出 CSV，按会员档限制）。

---

## 5. 横向公共能力

### 5.1 AI 助手（AI-Manager）

几乎每个模块都有，统一用同一个人设：

- **研判类**：在市场观察和 AI 投研里，只根据已生成的报告回答问题。
- **执行类**：在 TAA、FOF 增强、股票指数增强里按步骤带着用户做（共驾），或一键跑完（自动驾驶）；结果可导出 PDF、发布到竞技场。
- **生成类**：竞技场里用对话生成组合。

### 5.2 大模型网关与算力

- 平台转售多家大模型（GLM、Qwen、DeepSeek、豆包、PaddleOCR 等）。用户在平台签发 API Key 后，可以用 **OpenAI 兼容接口**从外部调用。
- 模型分免费和收费两类，收费模型按 Token 或按次计费；平台还维护共享算力池。
- **按功能场景路由模型**：每日点评、AI 投研、行业轮动等场景可以分别指定用哪个模型、哪个通道。这是值得借鉴的设计，模型可以替换，业务代码不用改。

### 5.3 数据与存储

| 库 / 集合 | 内容 |
| --- | --- |
| 行情 / 净值 | 基金复权净值和分类（level1 / level2）、指数日线、股票行情 |
| `cnafl_daily` | 股票传统因子（标普 Alpha 因子库），日度截面 |
| 券商因子库 | 国盛、东吴、开源、国金、广发的因子 |
| `fund_monthly` | 基金因子，月末截面，另有日频 `daily` |
| `cne5d.barra` | Barra CNE5 风格暴露 |
| `ga_factor.<方案>` | 基因工厂物化出的 GA 因子，以及 ML 合成因子的截面 |
| `res_bt.<方案>` | 所有回测结果，一个方案一个集合，方案名前缀有约定（fof / ga / mom / ga_factory_* 等） |
| 研报库 | 资产研报、行业研报、金股研报 |
| 高频 | Level-2 因子，Parquet 格式 |

数据源可以按用户覆盖：全站有一套默认的回测库和因子表，个人可以切换到自己的库，只对自己生效。
这样研究员能在私有库里做实验，不会污染全站默认的数据。

---

## 6. 商业化与权限（横切层，一笔带过）

权限是叠在业务之上的一层门控，不改变业务流本身：
模块开关由管理员控制，功能深度按四档年费逐级解锁（从「只看结果」到「自己跑 GA / ML / 优化器」，再到「自选因子库和回测库」）。
高级权限多数只是对更多因子库、更长数据、更重算力的许可，本质上还是同一条因子研究业务线。

收入来自年费、按大模型用量扣的平台币、数据下载配额。
拉新和留存靠打卡、竞技场社交、另类市场的年度奖励。

对自研平台来说，只需要在架构上预留**用户 → 角色 → 功能 / 数据集许可**这一层（见 §8.5），不必照搬它的计费设计。

---

## 7. 观察与评价

### 7.1 设计上值得借鉴的地方

1. **「实验室 / 组合」分离，按策略指纹续跑**。研究和运营被清楚地分开，历史净值不可篡改，跟单可以连续同步。
2. **所有策略都落到同一个组合层**，再接竞技场和模拟交易，产品闭环完整。
3. **回测结果统一落在 `res_bt`，一个方案一个集合**，因子回顾、优化器、下载都读同一份数据。
4. 每个模块都做了「专家工具箱」和「AI 一键模式」两套入口，同时照顾专业用户和小白用户。

### 7.2 问题和不一致

1. 同一个概念在不同页面口径不同。例如基金侧 ML 的说明写「没有验证集、不早停」，表单却有 Train/Val/Test 和早停；方法论页说 IC 就是 Spearman，结果库里却同时存了 `ic_mean` 和 `rankic_mean`。
2. 股票侧的 ML 合成目前固定用一个现成的因子包训练，**不读取用户放进备选池的因子**，和页面文案不一致。
3. 部分入口的命名和导航位置在迭代里没有同步。例如资产配置页写「行业轮动已归入公募 FOF」，而行业轮动其实已经是独立模块了。
4. 业务线很多（赛马、预测市场、视频生成模型……），核心的投研功能在导航里被稀释了。

---

## 8. 平台视角：量化投研的通用工作流架构

把这个平台的 12 个模块抽掉具体品种和包装，剩下的是一条业内通用的量化工作流。
下面给出这条工作流的阶段划分、三种「出权重」范式、核心对象模型和支撑它们的基础设施。

### 8.1 六阶段工作流

```text
① 数据 ──► ② 研究 / 信号 ──► ③ 策略构建 ──► ④ 组合管理 ──► ⑤ 执行 ──► ⑥ 监控 / 归因 / 复盘
   ▲                                                                             │
   └──────────────────────────── 反馈（记忆、再训练、参数迭代）◄────────────────────┘
```

| 阶段 | 要回答的问题 | 平台里对应的模块 | 典型产出 |
| --- | --- | --- | --- |
| ① 数据 | 有什么数据，是否干净、对齐、无未来函数 | 行情 / 净值 / 因子库 / 研报库 / Barra / 高频 · 数据下载 | 标准化面板数据 |
| ② 研究 / 信号 | 什么信号能预测收益，稳不稳 | 市场观察、AI 投研（主观信号）；GA、批测、因子回顾、风险因子批测（量化信号） | 因子、观点、强弱指数 |
| ③ 策略构建 | 信号怎么变成目标权重 | 因子合成 + 优化器；SAA 的 BL；TAA 倾斜；行业 DRL / LLM；CPPI / TIPP；MOM 选小 M | 目标权重序列 + 回测 |
| ④ 组合管理 | 权重怎么落成一个持续运行的组合 | 我的组合：构建、冻结参数、按指纹续跑、自动调仓 | 组合、调仓记录、净值 |
| ⑤ 执行 | 调仓怎么变成订单 | 全球交易（券商模拟盘）、公募交易、竞技场跟单同步 | 订单、成交、持仓 |
| ⑥ 监控 / 归因 / 复盘 | 赚的是什么钱，模型还有效吗 | 业绩展现（样本内外、分年、超额）、市场风格、配置回顾（命中率）、SAA 的记忆回流 | 报告、下一轮的参数 / 记忆 |

**关键判断**：前三个阶段属于**研究域**，追求可复现、可对比、可以大量试错；
后三个阶段属于**运营域**，追求连续、不可篡改、可审计。
这个平台用「实验室 / 我的组合」把两个域切开，这是整个架构里最值得借鉴的一刀。

### 8.2 三种「出权重」的范式

平台上六条策略线看起来各不相同，但归纳起来只有三种范式，最后都输出同一种东西：**某个日期的目标权重**。

| 范式 | 输入 | 核心算法 | 平台实例 |
| --- | --- | --- | --- |
| **因子驱动**（截面选股 / 选基） | 大量标的的因子截面 | 单因子检验 → 筛选 → 合成 → 带约束的优化 | 股票指数增强、公募 FOF 增强、私募 MOM（业绩因子） |
| **观点驱动**（大类 / 行业配置） | 宏观数据、研报文本、专家观点 | 多智能体打分 → 聚合 → BL / 风险平价倾斜 | SAA、TAA、行业轮动（LLM）、AI 投研 |
| **规则 / 学习驱动**（仓位控制） | 净值路径、少数资产的时序 | 组合保险公式；强化学习（PPO / SAC） | 固收+ CPPI / TIPP、行业轮动（DRL）、FOF 的 DRL |

因此所有策略可以统一成一个接口：

```text
Strategy(frozen_params).weights(as_of_date) -> {asset: weight}
```

组合层、回测、执行、跟单都只依赖这个接口，而不关心策略内部用的是哪种范式。
**新增一条策略线 = 实现一个新的 Strategy，下游模块都不用改。**

### 8.3 研究域的通用流水线

三种范式的研究过程可以套进同一个骨架（因子驱动最完整，另外两种是它的子集）：

```text
候选生成 ──► 批量评测 ──► 筛选入池 ──► 合成 ──► 约束优化 ──► 回测对照 ──► 保存方案
(传统库/GA/   (统一口径的    (门槛+去相关   (线性/ML/  (TE/行业/     (对基线：
 LLM/专家)     指标入结果库)  +限额)        DL/RL)     风格/个股)    等权/指数)
```

平台在每一步都遵守的三条纪律，自研时建议照搬：

1. **统一口径**：不同来源的候选（传统因子、GA 公式、ML 合成结果）进同一套批测、写进同一张结果表，才能放在一起比较。
2. **永远对基线**：ML 对等权合成，优化组合对市值加权指数，DRL 对等权 Baseline，LLM 行业策略对年调风险平价。
3. **样本内外分离**：GA 的 ITS / OFS、ML 的 Train / Val / Test、组合的「构建日前 / 构建日后」，都是同一个原则。

### 8.4 核心对象模型

平台的数据流可以用下面几类对象描述，这也是自研平台的数据库骨架：

```text
Dataset ──► Factor ──► Scheme(实验配置) ──► ResultSet ──► CandidatePool
                                                              │
                                                              ▼
                                  StrategySpec(冻结参数 + 指纹) ──► Portfolio ──► Rebalance ──► Order/Fill
                                                                       │
                                                                       └──► Publication ──► FollowLink(跟单)
```

| 对象 | 含义 | 平台中的体现 |
| --- | --- | --- |
| Dataset | 一份面板数据（行情、因子、暴露） | `cnafl_daily`、`fund_monthly`、`cne5d.barra` |
| Factor | 一个因子：来源、公式、方向、所属族 | 字典条目、GA 公式、ML 合成字段 |
| Scheme | 一次实验的全部配置（区间、池、周期、参数） | 方案名 = 结果集合名 |
| ResultSet | 一次实验的全部结果，key 为 `池\|风格\|因子` | `res_bt.<方案>` |
| CandidatePool | 用户勾选出来、准备进下一步的因子或产品 | Stock / FOF AI 备选池、FOF / MOM 待选池 |
| StrategySpec | 冻结的策略参数，以及由参数算出的指纹 | 「保存方案」、策略指纹 |
| Portfolio | 一个持续运行的组合 | 我的组合 |
| Rebalance | 一次调仓（日期、来源、权重），只追加 | 调仓记录 |
| Order / Fill | 订单与成交 | 模拟盘订单 |
| Publication / FollowLink | 公开发布、跟单关系 | 竞技场 |

### 8.5 基础设施

| 组件 | 职责 | 平台做法 | 自研建议 |
| --- | --- | --- | --- |
| 任务引擎 | 跑批测、GA、训练、回测这类长任务 | 后台单进程，前端轮询日志，同类任务互斥 | 任务队列（如 Celery / RQ）+ 任务状态表 + 日志流；允许并发，按资源限流 |
| 结果存储 | 存实验结果，并能查询、对比 | Mongo，一个方案一个集合 | 列式存储（Parquet / DuckDB）或数据库表 + 方案元数据表 |
| 优化器服务 | 带约束的组合优化 | MOSEK / CVXOPT | cvxpy 封装成统一的约束描述 |
| 调度器 | 定时抓数、生成报告、自动调仓 | 每天抓取、16:35 自动调仓 | cron / APScheduler，调仓前先校验数据是否就绪 |
| 模型网关 | 统一接入大模型，按场景路由 | 自建兼容 OpenAI 的网关 | 需要 AI 功能时再做；先把「场景 → 模型」配置化 |
| 权限层 | 用户、角色、功能与数据集许可 | 模块开关 + 会员分档 | 用户 → 角色 → 功能 / 数据集的许可表 |
| 数据覆盖 | 个人实验库和全站默认库分开 | 用户可切换回测库 / 因子表 | 命名空间隔离：`global` 与 `user:<id>` |

---

## 9. 自研平台蓝图

结合上面的通用架构，给自研平台的建议如下：模块边界照抄六阶段，建设顺序按「研究域 → 运营域 → 增值层」推进。

### 9.1 模块划分

```text
quant-platform/
├── data/          ① 数据：行情、股票池、行业、复权、交易日历、因子面板存取
├── research/      ② 研究：因子计算（Alpha101 等）、预处理、单因子测试、结果库、筛选
│   └── mining/        GA 挖掘（可选）
├── strategy/      ③ 策略：合成器（线性 / ML）、优化器、Strategy 接口、回测引擎
├── portfolio/     ④ 组合：StrategySpec 冻结、指纹、续跑、调仓记录、净值
├── execution/     ⑤ 执行：订单生成、模拟撮合，以后再接券商
├── monitor/       ⑥ 监控：业绩归因、风格暴露、因子衰减监控、报告
└── infra/         任务引擎、调度、配置、权限、（可选）大模型网关
```

### 9.2 分期路线

| 期 | 目标 | 范围 | 完成标志 |
| --- | --- | --- | --- |
| P0 | 因子研究闭环 | data + research：Alpha101 计算、预处理、按股票池和预测周期的单因子测试、结果库、族内筛选 | 能输出一张和本平台「因子回顾」同口径的表 |
| P1 | 策略与回测 | strategy：等权 / ICIR 合成、LightGBM 合成、cvxpy 指数增强优化器、统一的 Strategy 接口 | 能对 300 / 500 / 1000 跑出逐年超额、TE、回撤 |
| P2 | 组合运营 | portfolio + monitor：冻结参数、指纹续跑、只追加调仓、样本内外净值、Barra 归因 | 每月自动产出下期权重，历史净值不可改写 |
| P3 | 执行 | execution：订单生成、模拟盘，之后接真实券商 | 权重 → 订单 → 持仓对账跑通 |
| P4（可选） | 增值 | 观点驱动策略（LLM 研报打分）、GA 挖掘、多用户与权限、社交分享 | 按需要取舍 |

### 9.3 可以不做的部分

市场资讯聚合、AI 写日报、赛马和预测市场、会员计费、竞技场社交，都是这个平台面向 C 端做的增值和运营功能。
和量化投研的核心工作流无关，自研平台可以放到最后再考虑，或者不做。

因子流水线每一步的具体做法和默认参数，见 👉 [因子挖掘专题](factor_mining_topic.md)。

---

## 附录 A：导航全表

| 顶栏模块 | 子功能 |
| --- | --- |
| 首页 | 功能地图、每日速览、AI 助手入口、热门公开组合 |
| 市场观察 | 每日点评、共识前瞻、资产观察、国内媒体、全球视角、市场温度计、宏观交易 |
| 资产配置 | 工作台、AI 助手、历史研报、配置回顾、战略资产配置、战术资产配置、组合构建、业绩展现 |
| 公募FOF | 工作台、AI 助手、历史回顾、查基金、选经理、遗传算法、GA 因子初筛、基因工厂、因子批测、因子回顾、机器学习、线性加权、增强优化器、组合构建、业绩展现 |
| 行业轮动 | 工作台、深度强化学习、大语言模型、组合构建、业绩展现 |
| 股票增强 | 工作台、AI 助手、历史回顾、查股票、市场风格、风险因子批测、遗传算法、GA 因子初筛、基因工厂、量化因子批测、传统因子回顾、GA 因子精选回顾、高频因子总览、AI 因子合成、线性加权、单因子策略、多因子策略、组合构建、业绩展现 |
| 固收+ | CPPI（国内 / 海外）、TIPP（国内 / 海外）、组合构建、业绩展现 |
| 私募FOF | 工作台、策略总览、查产品、比业绩、私募批量回测、组合构建、业绩展现 |
| 竞技场 | AI 助手（生成式策略）、发现组合、交易员、动态、我的跟单 |
| 全球交易 | 券商模拟盘（主观 / 量化 / 宏观）、公募交易（主观 / 量化） |
| 另类市场 | 赛马（交易台、我的组合、马匹榜、骑师榜、练马师榜、搜索）、未来事件（未来故事、事件交易所、我的组合） |
| 帮助和数据 | 板块说明、会员权限、大模型、因子方法、基金/股票/大类资产因子字典、GA / SGP-LSTM / BayGA 说明、竞技场说明、组合说明、高频数据说明、数据下载 |

## 附录 B：竞技场品类的自动映射

| 组合来源 | 竞技场品类 |
| --- | --- |
| 资产配置 SAA / TAA | 平衡型 FOF |
| 行业轮动 | 行业轮动 FOF |
| 公募 FOF 增强 | 权益增强 FOF |
| 股票策略 | 股票增强 |
| 固收+ CPPI / TIPP | 固收+ FOF |
| 私募 MOM | 私募 FOF |
